{
  "name": "ikea-wishlist",
  "private": true,
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "dev": "pnpm --filter @wishlist/web dev",
    "dev:api": "pnpm --filter @wishlist/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
